import apiClient, { config, authConfig } from "./Env";
import CPNetwork from "./CPNetwork";
import Urls from "./Urls";

const baseUrl = "https://dashboard.clubpassports.com/";
const oldUrl = "https://www.clubpassport.hexaademo.com/";

export { apiClient, config, authConfig, CPNetwork, Urls, baseUrl, oldUrl };
